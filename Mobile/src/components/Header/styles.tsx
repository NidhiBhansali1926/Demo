import { StyleSheet } from 'react-native';

import { Margin } from '@theme';

const styles = StyleSheet.create({
  container: {
    marginTop: Margin.large + Margin.extraTiny,
  },
  title: {
    marginTop: Margin.small,
  },
});

export default styles;
